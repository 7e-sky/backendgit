<h3>Bonjour</h3>
{% if  demande.type %}
    <p>
        <strong>{{ demande.fournisseur.societe }}</strong> vient de faire une demande de renouvellement pour l'offre "<strong>{{ demande.offre.name }}</strong>",
        prière de bien vouloir le contacter dans les plus brefs délais.
    </p>
{% else %}
    <p>
       <strong>{{ demande.fournisseur.societe }}</strong> vient de faire une commande pour l'offre "<strong>{{ demande.offre.name }}</strong>",
        prière de bien vouloir le contacter dans les plus brefs délais.
    </p>
{% endif %}

<h4>Les informations du fournisseur :</h4>
<table cellpadding="0" cellspacing="0" border="0"
       style="width: 62% !important; min-width: 40%; max-width: 62%;">
    <tr>
        <td><strong>Raison sociale</strong></td>
        <td> : {{ demande.fournisseur.societe }} </td>
    </tr>
    <tr>
        <td><strong>Nom complet</strong></td>
        <td> : {{ demande.fournisseur.civilite~' '~demande.fournisseur.firstName~' '~demande.fournisseur.lastName }}</td>
    </tr>
    <tr>
        <td><strong>Téléphone</strong></td>
        <td> :  <a href="tel:{{ demande.fournisseur.phone }}">{{ demande.fournisseur.phone }}</a></td>
    </tr>

    <tr>
        <td><strong>Fixe</strong></td>
        <td> :  <a href="tel:{{ demande.fournisseur.fix }}">{{ demande.fournisseur.fix }}</a></td>
    </tr>
    <tr>
        <td><strong>Email</strong></td>
        <td> : {{ demande.fournisseur.email }}</td>
    </tr>
    <tr>
        <td><strong>Pays</strong></td>
        <td> : {{ demande.fournisseur.pays ? demande.fournisseur.pays.name : '' }}</td>
    </tr>

    <tr>
        <td><strong>Ville</strong></td>
        <td> : {{ demande.fournisseur.ville ? demande.fournisseur.ville.name:'' }}</td>
    </tr>

    <tr>
        <td><strong>Adresse</strong></td>
        <td> : {{ demande.fournisseur.adresse1 }}</td>
    </tr>
</table>

<br>
<h4>Les informations de la commande :</h4>

<table cellpadding="0" cellspacing="0" border="0"
       style="width: 62% !important; min-width: 40%; max-width: 62%;">
    <tr>
        <td><strong>Offre</strong></td>
        <td> : {{ demande.offre.name }} </td>
    </tr>
    <tr>
        <td><strong>Prix</strong></td>
        <td> : {{ demande.prix~' '~demande.currency }}</td>
    </tr>
    <tr>
        <td><strong>Mode de paiement</strong></td>
        <td> :  {{ demande.mode.name }}</td>
    </tr>

    <tr>
        <td><strong>Durée</strong></td>
        <td> :  {{ demande.duree.name }} mois <span style="color: red;">{{ demande.duree.remise ? '(Remise '~demande.duree.remise~' %)' : '' }}</span></td>
    </tr>
    <tr>
        <td><strong>Date de création</strong></td>
        <td> : {{ demande.created|date("d/m/Y")}}</td>
    </tr>
    <tr>
        <td><strong>Activités</strong></td>
        <td> : {% for sousSecteur in demande.sousSecteurs %}
                {{ sousSecteur.name~' ' }}
            {% endfor %}</td>
    </tr>

</table>

